version: '3.8'

services:
  app:
    build: .
    ports:
      - "3141:3141"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3141
    restart: unless-stopped
